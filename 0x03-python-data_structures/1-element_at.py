#!/usr/bin/python3
def element_at(my_list, idx):
    length = len(str(my_list))
    id = my_list[idx]
    if idx < 0:
        return none
    if idx > length:
        return none
    else:
        return id
